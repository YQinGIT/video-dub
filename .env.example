# Copy to .env and fill in. .env is git-ignored.
# Every setting uses the VIDEODUB_ prefix; see src/videodub/config.py.

# DeepSeek API key — translation stage (Stage 4).
VIDEODUB_DEEPSEEK_API_KEY=

# ElevenLabs API key — optional TTS fallback backend (Stage 7c).
VIDEODUB_ELEVENLABS_API_KEY=
